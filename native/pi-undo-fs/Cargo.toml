[package]
name = "pi-undo-fs"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
hex = "0.4"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
